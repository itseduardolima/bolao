# SEO — Implementação por Fases

> Status: Fases 1–5 implementadas (falta a 6, opcional) · Data: 2026-06-25

## Objetivo

Sair do zero de SEO. Hoje só existe o que o Next gera sozinho + metadata em `/termos` e `/privacidade`. O objetivo, em ordem de impacto **para um bolão** (cujo crescimento é viral por link de convite no WhatsApp):

1. **Preview de link** (Open Graph + imagem) — converter convites colados no WhatsApp.
2. **Indexação controlada** — `robots` + `sitemap` + `noindex` nas páginas privadas.
3. **Títulos/descrições únicos** por página.
4. **Dados estruturados** (JSON-LD) para rich results.
5. **PWA/ícones/mobile**.
6. **Conteúdo orgânico** (landing) — opcional, maior esforço.

Sem mudança de comportamento das páginas. Cada fase = 1 commit, `tsc --noEmit` + `eslint` limpos.

---

## Diagnóstico (inventário)

| Item | Status hoje |
|---|---|
| `lang="pt-BR"` | ✅ |
| `title`+`description` root (genérico) | ✅ |
| Metadata `/termos`, `/privacidade` | ✅ |
| `favicon.ico` | ✅ |
| `metadataBase` | ❌ |
| `openGraph` / `twitter` | ❌ (em tudo) |
| `opengraph-image` | ❌ |
| `sitemap.ts` | ❌ |
| `robots.ts` | ❌ |
| `title.template` | ❌ |
| Metadata em home, `jogos`, `jogos/[id]`, `grupos`, `perfil`, `pontuacao`, `onboarding` | ❌ (herdam título genérico) |
| `noindex` em páginas privadas | ❌ |
| JSON-LD | ❌ |
| `viewport` / `theme-color` / `manifest` | ❌ |
| `apple-icon` / `icon.png` | ❌ |
| `/public` com lixo do template (`next.svg`, `vercel.svg`, etc.) | ⚠️ limpar |

Convenções confirmadas contra `node_modules/next/dist/docs/` (Next 16): `metadataBase`, `title.template`, file-conventions `sitemap.ts`/`robots.ts`/`opengraph-image.tsx`, `export const viewport`, `manifest.ts`, `next/og` (`ImageResponse`).

---

## Fase 0 — Decisões e pré-requisitos (sem código) ✅ decidido

1. **Domínio de produção** → `https://bolao-conectados.vercel.app` (Vercel). `NEXT_PUBLIC_SITE_URL` criada no `.env.local` (e a definir na Vercel); `src/lib/site.ts` tem fallback para essa URL.
2. **Imagem OG**: ✅ **gerada com `next/og`** (`ImageResponse`) — habilita o convite personalizado.
3. **Landing orgânica (Fase 6)**: adiada.

---

## Fase 1 — Open Graph & compartilhamento  ⭐ prioridade máxima ✅ feito

**Por quê:** link de convite (`/grupos/entrar/[code]`) colava no WhatsApp sem nenhuma prévia → matava conversão.

Implementado:
- `src/lib/site.ts` (novo): `SITE_URL/HOST/NAME/TITLE/DESCRIPTION` (lê `NEXT_PUBLIC_SITE_URL` com fallback).
- `src/app/layout.tsx`: `metadataBase`, `title.template` (`%s · Bolão Copa 2026`), `description`, `openGraph` e `twitter` (`summary_large_image`).
- `src/app/opengraph-image.tsx` (novo): imagem global 1200×630 on-brand (`next/og`, logo + cores).
- `src/lib/groups.ts`: `getInviteGroupPreview(code)` cacheado (nome + nº de membros).
- `src/app/grupos/entrar/[code]/page.tsx`: `generateMetadata` ("Entre na liga X") com `noindex`.
- `src/app/grupos/entrar/[code]/opengraph-image.tsx` (novo): preview dinâmico com nome da liga + "N amigos já estão competindo".
- `.env.local`: `NEXT_PUBLIC_SITE_URL` (definir também na Vercel).
- **`src/middleware.ts`** (correção necessária): o matcher gateava `/opengraph-image` → crawler deslogado caía no login e o preview sumia. Adicionado `isMetadataRoute` liberando OG/Twitter image, `sitemap.xml`, `robots.txt`, `manifest.webmanifest`, ícones — já cobre Fases 2 e 5.

**Validado localmente:** OG global e de convite renderizam PNG 1200×630 (HTTP 200) com nome real da liga; `<title>`, `og:*`, `twitter:*` e `noindex` do convite corretos no HTML. **Pendente (manual):** definir `NEXT_PUBLIC_SITE_URL` na Vercel, deploy e conferir no [opengraph.xyz](https://www.opengraph.xyz/) / WhatsApp.

---

## Fase 2 — Indexação: robots + sitemap + noindex ✅ feito

Implementado:
- `src/app/robots.ts`: `allow: /`; `disallow: /api/, /onboarding, /perfil, /grupos/pagamento`; `host` + `sitemap`.
  - ⚠️ **Decisão:** NÃO bloquear `/grupos` (pegaria `/grupos/entrar`) nem rotas `*/opengraph-image` — o crawler do WhatsApp/Facebook respeita robots.txt e precisa buscar o preview do convite (Fase 1).
- `src/app/sitemap.ts`: 5 rotas estáticas + `/jogos/[id]` por jogo (query Prisma, `revalidate = 3600`). Validado: 77 URLs.
- `noindex` (`robots: { index:false, follow:false }`) nas privadas server: `perfil`, `grupos`, `grupos/[id]`.
  - `grupos/pagamento` e `onboarding` são **client components** (não exportam `metadata`) → cobertos pelo `disallow` do robots + redirect de auth.
- `alternates.canonical`: home (`/`, evita `?login=1&from=…`) e `/jogos` (consolida `?date=`). Validado no HTML.

Observação: as privadas já são 307→login para crawler deslogado, então o `noindex` é reforço; o valor real da fase está no sitemap (descoberta de `/jogos/[id]`) e nos canonicals.

---

## Fase 3 — Metadata por página (títulos únicos) ✅ feito

- `src/lib/games.ts` (novo): `getGameSummary(id)` cacheado.
- `/jogos/[id]`: `generateMetadata` → title `Time A x Time B`, description (mostra placar se finalizado), canonical, `openGraph`/`twitter`.
- `/jogos/[id]/opengraph-image.tsx` (novo): OG por jogo (placar grande se finalizado, ou "VS" + data).
- `/jogos`: title + description + canonical (feito na Fase 2).
- `/pontuacao`: title + description + canonical.
- Títulos nas privadas (tab UX, já `noindex`): `perfil` ("Meu perfil"), `grupos` ("Minhas ligas"), `grupos/[id]` ("Detalhes da liga").
- Home usa o `default` do template (já bom); `/termos` e `/privacidade` já prontos.

---

## Fase 4 — Dados estruturados (JSON-LD) ✅ feito

- `src/components/seo/JsonLd.tsx` (novo): componente server (`<script type="application/ld+json">`).
- Root layout: `@graph` com `WebSite` + `Organization` (logo → `/icon`).
- `/jogos/[id]`: `SportsEvent` (times, `startDate`, local, `eventStatus`) + `BreadcrumbList` (Início › Jogos › confronto).
- Validado em produção: tipos `WebSite`, `Organization`, `SportsEvent`, `SportsTeam`, `BreadcrumbList`, `ListItem` no HTML.

---

## Fase 5 — PWA / ícones / mobile ✅ feito

- `export const viewport` no layout (`themeColor #0f0f1a`, `width=device-width`). Validado: `<meta name="theme-color">`.
- `src/app/manifest.ts` (novo) → `/manifest.webmanifest` (standalone, theme/background `#0f0f1a`, ícone `/icon` 512² any+maskable).
- `src/app/icon.tsx` (512²) + `src/app/apple-icon.tsx` (180²) — `next/og`, troféu sobre fundo da marca. Auto-injetados; `/icon` também serve o ícone do manifest.
- `/public` limpo (removidos `next/vercel/globe/window/file.svg`).
- **Validação:** dev server do usuário estava em estado inconsistente (Turbopack, "require is not defined" em páginas até não-tocadas) → validei com **build de produção limpo + `next start`**: todas as páginas e rotas de metadata 200, OG/ícones renderizados.

> ⚠️ **Deprecação pendente (fora de escopo SEO):** o build avisa `middleware` → `proxy` (Next 16). `src/middleware.ts` ainda funciona, mas convém renomear para `src/proxy.ts` num passo dedicado (é auth-gating — merece teste próprio).

---

## Fase 6 — Conteúdo orgânico (opcional, maior esforço)

- Landing para visitante **não-logado** na home: copy + headings mirando "bolão copa do mundo 2026", "palpites copa", explicando o produto (hoje a home é `force-dynamic` e mostra só o ranking, sem conteúdo de captação).
- Avaliar trocar `force-dynamic` por ISR/`revalidate` em `/jogos` (cache + bom para crawler).

---

## Regras

- Cada fase = 1 commit; `tsc --noEmit` + `eslint` limpos.
- Sem regressão visual nem de comportamento.
- Validar OG real no [opengraph.xyz](https://www.opengraph.xyz/) e no validador do WhatsApp/Twitter após Fase 1.
- Submeter `sitemap` no Google Search Console após Fase 2 (passo manual fora do código).

## Decisões em aberto

- [ ] Domínio de produção (`NEXT_PUBLIC_SITE_URL`).
- [ ] OG estática vs gerada (`next/og`).
- [ ] Fazer a landing da Fase 6 ou deixar para depois.
